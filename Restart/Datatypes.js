"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high and this type of writing is not acceptable

console.log("Shraddha")


let name = "SHRADDHA" //it's type is string
let age = 18 //it's type is number
let isLoggedIn = false //it's type is boolean
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // it's type is undefined
console.log(typeof null); //it's type is object